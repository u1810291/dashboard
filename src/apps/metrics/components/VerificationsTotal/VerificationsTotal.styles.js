import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  titleNumber: {
    fontSize: '3rem',
    fontWeight: 400,
  },
  number: {
    fontSize: '1.25rem',
  },
  title: {
    fontSize: '1rem',
    color: 'gray',
  },
}));

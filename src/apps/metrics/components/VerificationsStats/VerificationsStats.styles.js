import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  title: {
    fontWeight: 'bold',
    fontSize: '1.25rem',
    marginBottom: '1rem',
  },
  card: {
    position: 'relative',
  },
}));

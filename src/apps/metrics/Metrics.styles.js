import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  header: {
    gridColumn: [['span', '3']],
  },
  graph: {
    gridColumn: [['span', '2']],
  },
}));

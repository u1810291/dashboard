import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
  wrapper: {
    padding: [[12, 20]],
    borderRadius: 5,
  },
  title: {
    paddingRight: 20,
    fontWeight: 'bold',
    fontSize: 21,
  },
  text: {
    fontSize: 14,
  },
});

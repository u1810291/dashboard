import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  title: {
    paddingTop: 120,
    lineHeight: 29,
  },
  text: {
    paddingBottom: 60,
    lineHeight: '133.3%',
  },
}));

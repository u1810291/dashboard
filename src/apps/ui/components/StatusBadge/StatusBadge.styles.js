import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
  root: {
    position: 'absolute',
    right: -3,
    top: 0,
    minWidth: 100,
    borderRadius: 30,
    textAlign: 'center',
    transform: 'translateY(-50%)',
  },
});

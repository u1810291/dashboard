import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
  badge: {
    alignItems: 'center',
    background: '#FEC649',
    borderRadius: 3,
    color: '#232939',
    display: 'flex',
    flexGrow: 0,
    fontSize: '12px',
    fontWeight: 'bold',
    justifyContent: 'center',
    lineHeight: '14px',
    maxWidth: 40,
    minWidth: 'auto',
    padding: [[0, 3]],
  },
});

import makeStyles from '@material-ui/core/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  previewIframe: {
    border: 0,
    height: '100%',
    width: '100%',
    minHeight: '600px',
  },
}));

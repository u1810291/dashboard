import makeStyles from '@material-ui/core/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  root: {
    width: 600,
    padding: '20px 35px',
  },
  previewRoot: {
    width: 480,
  },
}));

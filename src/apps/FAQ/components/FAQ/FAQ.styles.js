import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
  emailLink: {
    color: '#507DED',
    textDecoration: 'underline',
    paddingTop: 10,
    fontWeight: 'bold',
  },
  list: {
    marginBottom: 20,
  },
});

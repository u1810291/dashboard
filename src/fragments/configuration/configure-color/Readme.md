```js
const presets = [['#000000', 'blue'], ['#333333', 'green'], ['#666666', 'red']]

initialState = {
  color: 'red'
}
;<ConfigureColor presets={presets} style={initialState} onClick={setState} />
```

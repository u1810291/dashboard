Note that face verification is added by default.  
See `AVAILABLE_DOCUMENT_TYPES` from `src/state/merchant` for document types list.

```jsx
<DocumentTypesLabel types={['passport', 'national-id']} />
```
